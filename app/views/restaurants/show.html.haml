%p1= @menu.name
%br
- @wines.each do |wine|
  = link_to "#{wine.vintage} #{wine.country} #{wine.region} #{wine.purveyor} #{wine.grape} #{wine.style} #{wine.btg} #{wine.btb}", wine_path(wine)
  %br
%br
%br
= link_to "Recommendations", wines_recommendations_path(@menu), class: 'btn btn-success'

